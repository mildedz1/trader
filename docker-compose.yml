services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: mexc-telegram-trader:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: ["python", "-m", "app"]
